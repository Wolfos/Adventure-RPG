namespace Utility
{
	// Needs to match the actual database in order. Lookup is int-based so the name doesn't need to match
	public enum DatabaseCategory
	{
		Default = 0,
		Items = 1,
		Characters = 2,
		Shops = 3,
		StatusEffects = 4
	}
}